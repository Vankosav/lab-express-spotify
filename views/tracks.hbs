{{#each tracks}}
  <h2>{{this.name}}</h2>
  {{#each this.artists}}
    <h2>{{this.name}}</h2>
    {{#if this.preview_url}}
      <audio controls src="{{this.preview_url}}"></audio>
    {{else}}
      <p>No preview available for this track.</p>
    {{/if}}
  {{/each}}
{{/each}}